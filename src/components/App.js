import React from 'react';
import Normalize from 'react-normalize';

import MainPage from './mainPage/MainPage';

function App() {
  return (
    <div>
      <Normalize />
      <MainPage />
    </div>
  );
}

export default App;
